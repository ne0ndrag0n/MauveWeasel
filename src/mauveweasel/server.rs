use std::fs::File;
use std::io::Read;
use toml;

pub struct MauveWeaselServer {

}

impl MauveWeaselServer {
    pub fn new() -> MauveWeaselServer {
        MauveWeaselServer {

        }
    }

    pub fn run( &self ) {

    }
}
